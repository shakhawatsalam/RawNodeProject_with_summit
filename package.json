{
  "name": "raw-node-project-with-sumit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "SET \"NODE_ENV=staging\" && nodemon index",
    "production": "SET \"NODE_ENV=production\" && nodemon index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-modules": "^1.0.1",
    "nodemon": "^2.0.20"
  }
}
